#include <iostream>
#include <string>
#include <thread>

#include <SFML/Network.hpp>

#include "NetEngine.h"

#define NETENGINE nsNetEngine::NetEngine

using namespace std;

NETENGINE::NetEngine(const std::string &p_ipAdress, const unsigned int &p_port, const bool &p_isServer) noexcept
{
    m_ipAdress = p_ipAdress;
    m_port = p_port;
    m_isServer = p_isServer;
}

NETENGINE::~NetEngine() noexcept
{
}

void NETENGINE::launch(std::string p_mapName, unsigned int p_player)
{
    // The game instance is a player that hosts a game
    if (isServer)
    {
        
    }
}

NetPackage NETENGINE::reveive()
{
}

void NETENGINE::send(NetPackage package)
{
}

